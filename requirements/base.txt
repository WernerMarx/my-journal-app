# Runtime dependencies (shared by all environments).
# Pinned with compatible-release (~=) to allow patch updates only.

# --- Core framework ---
Django~=5.2.0
djangorestframework~=3.16.0

# --- Authentication ---
djangorestframework-simplejwt~=5.5.0      # JWT access/refresh tokens (single API token system)
dj-rest-auth~=7.0.0                        # auth endpoints (login/logout/password/registration)
django-allauth~=65.4.0                     # account workflows + future social providers
requests~=2.32.0                           # required by allauth's socialaccount/registration imports

# --- API utilities ---
django-cors-headers~=4.7.0                 # CORS management
drf-spectacular~=0.28.0                    # OpenAPI 3 schema + Swagger/Redoc
django-filter~=25.1                        # queryset filtering for DRF

# --- Async tasks ---
celery~=5.4.0                              # task queue
redis~=5.2.0                               # broker/result backend client

# --- Configuration & database ---
django-environ~=0.12.0                     # 12-factor env var configuration
psycopg[binary]~=3.2.0                     # PostgreSQL driver (psycopg 3)

# --- Production server / static files ---
gunicorn~=23.0.0
whitenoise~=6.9.0
